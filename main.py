import os
from langchain_google_genai import ChatGoogleGenerativeAI

os.environ["GOOGLE_API_KEY"] = os.getenv("GEMINI_API_KEY")

llm = ChatGoogleGenerativeAI(
    model="gemini-2.5-flash-lite",
    temperature=0.7,
)

topic = input("Введіть тему курсу: ")
audience = input("Опишіть цільову аудиторію: ")

# prompt = f"""
# Створи план навчального курсу на основі теми та цільової аудиторії.
#
# Ось приклади того, як потрібно формувати результат.
#
# Приклад 1:
#
# Тема: Python для початківців
# Цільова аудиторія: люди без досвіду програмування
#
# Результат:
# 1. Вступ до Python
#    - встановлення Python
#    - змінні та типи даних
#    - введення та виведення даних
# 2. Умови та цикли
#    - if/elif/else
#    - цикли for та while
#    - практичні задачі
# 3. Функції
#    - створення функцій
#    - параметри та аргументи
#    - повернення результатів
# 4. Робота зі структурами даних
#    - списки
#    - словники
#    - кортежі
# 5. Підсумковий проєкт
#    - створення невеликої програми на Python
#
# Приклад 2:
#
# Тема: Основи веброзробки
# Цільова аудиторія: початківці, які знають бази HTML
#
# Результат:
# 1. Основи CSS
# 2. Flexbox та Grid
# 3. Адаптивна верстка
# 4. JavaScript
# 5. Робота з API
# 6. Підсумковий вебпроєкт
#
# Тепер створи план для:
#
# Тема: {topic}
# Цільова аудиторія: {audience}
#
# Дотримуйся структури прикладів, але адаптуй зміст саме під задану тему та аудиторію.
# План повинен поступово переходити від простих тем до складніших.
# """
#
# response = llm.invoke(prompt)
#
# print("\nПлан навчального курсу:\n")
# print(response.content)
#
prompt = f"""
Створи детальний план навчального курсу.

Тема курсу: {topic}
Цільова аудиторія: {audience}

План повинен містити:
- назву курсу;
- мету курсу;
- перелік модулів;
- теми кожного модуля;
- короткий опис кожної теми;
- практичні завдання;
- підсумковий проєкт або завдання.

Врахуй рівень підготовки цільової аудиторії.
Зроби курс логічним та поступовим — від простого до складнішого.
"""

response = llm.invoke(prompt)

print("\nПлан навчального курсу:\n")
print(response.content)